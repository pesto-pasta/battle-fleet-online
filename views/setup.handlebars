<div id="Page-Game">
    <div id="Game" data-game-id="{{gameID}}">
        <div class="mode">

        </div>
        <div class="main">
            <div class="side" id="Opponent">
                <h2>Prepare For Battle</h2>
                <div class="board" id="myBoard"></div>
            </div>
            <div id="confirm-placements-button">

                <button id="confirm">Confirm Placements</button>

            </div>
        </div>
    </div>
</div>
<script src="/js/game_board.js"></script>
<script src="https://unpkg.com/axios@0.19.0/dist/axios.min.js"></script>
<script>

    const myBoard = document.getElementById("myBoard")
    
    const gameID = document.getElementById("Game").dataset.gameId;
    const confirmButton = document.getElementById("confirm");


    console.dir(myBoard);




    const gameBoard = new GameBoard(myBoard);

    

    //compile and send shipsPositions in body
    confirmButton.addEventListener("click", () => {
        const ships = gameBoard.getShips();
        axios.post('/confirm_placements/' + gameID, );
    })

    const shipsToPlace = [{
        size: 4,
        imgSrc: '/img/fleet/standard/4.png'
    }, {
        size: 3,
        imgSrc: '/img/fleet/standard/3.png'
    }, {
        size: 5,
        imgSrc: '/img/fleet/standard/5.png'
    }];

    gameBoard.startPlaceShip(5, '/img/fleet/standard/5.png');

    gameBoard.addEventListener('shipPlaced', (ship) => {
        gameBoard.addShips(ship);
        gameBoard.stopPlaceShip();

    })

    gameBoard.addEventListener('shipClicked', (ship) => {
        console.log("clicked", ship);

    });

</script>